server:
  port: 18080

spring:
  datasource:
    url: jdbc:mysql://localhost:13306/lkdcode
    username: root
    password: lkdcode

  redis:
    host: localhost
    port: 16379

  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    baseline-on-migrate: true
    baseline-version: 1
    enabled: true
    locations: classpath:db/migration

  sql:
    init:
      mode: never